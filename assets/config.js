/* Filled in from the Supabase project: Settings → API.
   The publishable key is meant to be public (Supabase's security model
   relies on Row Level Security, not on hiding this key), so it's safe to
   commit as-is. Never put the Secret key here — this file ships to the
   browser. */
window.CADENCE_CONFIG = {
  SUPABASE_URL: 'https://tlylnalyfdoupecounok.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_GagpBomPZN7Nw5AabvV8QQ_FVhdMJ9c',
};
